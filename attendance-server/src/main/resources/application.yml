app:
  jwt:
    secret: aVeryLongAndComplexSecretKeyForAttendanceManagementSystem123456
    expiration-ms: 3600000
  frontend:
    # NEW: CORS Configuration
    origin: http://localhost:3000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update